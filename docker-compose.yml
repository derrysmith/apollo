version: "3.2"

services:
  apollo-sql:
    container_name: apollo-sql
    image: mcr.microsoft.com/mssql/server:2017-latest
    ports:
      - "1433:1433"
    environment:
      SA_PASSWORD: "MsSqlP@ssw0rd"
      ACCEPT_EULA: "Y"