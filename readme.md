# apollo

.NET Core libraries for setting up your DDD entities to easily implement repositories against different data storage providers, and for executing domain logic using command handler patterns.